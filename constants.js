const IP = "localhost";
const PORT = 50541;
const CLIENT_NAME = "RJS";

//will add movement keys to this as well shortly, just have to change my input file first

module.exports = {
  IP,
  PORT,
  CLIENT_NAME,
};
